pipeline {
    agent{
        label "jenkins-agent"
    }

    environment {
        DOCKER_BUILDKIT = 1
    }

    stages {

        stage('CleanupWorkspace and Containers') {
            steps {
                cleanWs()
                sh 'docker-compose down'
                sh 'docker system prune -f'
            }
        }

        stage('Checkout from SCM') {
            steps {
                git branch: 'master', credentialsId: 'github', url: 'https://github.com/MaxNickerson/react-go-webapp'
            }
        }

       stage('Build and Test Backend') {
            steps {
                script {
                    dir('backend') {
                        sh 'docker-compose up --build backend'
                    }
                }
            }
        }

        stage('Build and Test Frontend') {
            steps {
                script {
                    dir('frontend') {
                        sh 'docker-compose up --build frontend'
                    }
                }
            }
        }

            
        

        stage('Deploy') {
            steps {
                script {
                    sh 'docker-compose up -d'
                }
            }
        }
    }

}